{% extends "MFBAdminBundle::base.html.twig" %}

{% block title %}Setup wizard{% endblock %}

{% block content %}

<div class="formular container modal-spacing">
    <h3 class="text-center form-header">{% trans %} Select Business type {% endtrans %}</h3>

    {{ form_start(form) }}

    <div class="form-group">
        <div class="col-xs-9">
            {% for type in form.businessType %}
                <div class="input-group">
                    <label class="radio-inline">
                        {{ form_widget(type, {'attr': {'class' : 'radio'} }) }} {{ type.vars.label }}
                    </label>
                </div>
            {% endfor %}
        </div>
    </div>

    <div class="form-group">
        <div class="col-xs-12">
            {{ form_widget(form.submit, {'attr': {'class': 'btn col-xs-12 form-control'} }) }}
        </div>
    </div>



    {{ form_end(form) }}

</div>


{% endblock %}

{% block form_errors %}
    {% spaceless %}
        {% if errors|length > 0 %}
            <div class="alert alert-danger text-center">
                {{ errors[0].message }}
            </div>
        {% endif %}
    {% endspaceless %}
{% endblock form_errors %}
